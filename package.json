{
  "name": "glyph-health",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=glyph-web",
    "build": "npm run build --workspace=glyph-web",
    "lint": "npm run lint --workspace=glyph-web",
    "test": "npm run test --workspaces --if-present",
    "type-check": "npm run type-check --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
