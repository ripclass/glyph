{
  "name": "@kham/schemas-clinical",
  "version": "0.1.0",
  "private": true,
  "description": "Zod schemas for Glyph clinical Verifiable Credential payloads (credentialSubject.data). Domain content only — the VC envelope, signing, and DID live in @kham/identity.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
