{
  "name": "@kham/identity",
  "version": "0.1.0",
  "private": true,
  "description": "Shared W3C identity envelope (Ed25519, did:web, Verifiable Credentials, JCS) for KhaM Health / Glyph and the Enso Identity Network. Persistence-agnostic crypto + document core — no database, no framework.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/ed25519": "^3.1.0",
    "@noble/hashes": "^2.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
